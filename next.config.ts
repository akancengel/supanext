import type { NextConfig } from "next";

const nextConfig: NextConfig = {
    // reactStrictMode: true,
    // swcMinify: false, // ← HTML ve JS minify işlemini kapatır    
    /* config options here */
};

export default nextConfig;
